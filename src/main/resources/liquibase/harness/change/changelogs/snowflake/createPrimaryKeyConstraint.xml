<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Include init.xml for cleanup -->
    <include file="liquibase/harness/change/changelogs/snowflake/init.xml"/>

    <!-- Create table without primary key initially -->
    <changeSet author="test-harness" id="setup-createPrimaryKeyConstraint">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="products"/>
            </not>
        </preConditions>
        <createTable tableName="products">
            <column name="id" type="int">
                <constraints nullable="false"/>
            </column>
            <column name="name" type="varchar(100)">
                <constraints nullable="false"/>
            </column>
            <column name="price" type="decimal(10,2)"/>
        </createTable>
    </changeSet>

    <!-- Add primary key constraint -->
    <changeSet author="test-harness" id="1">
        <addPrimaryKey tableName="products"
                      columnNames="id"
                      constraintName="pk_products"/>
        <rollback>
            <dropPrimaryKey tableName="products" constraintName="pk_products"/>
        </rollback>
    </changeSet>

</databaseChangeLog>